class Article {
    constructor(title, content, date, image) { //definimos los valores iniciales
        this.title = title;
        this.content = content;
        this.date = null;
        this.image = image
    }
}

export default Article;